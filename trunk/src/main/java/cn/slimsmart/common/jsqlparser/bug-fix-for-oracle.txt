1.修复AS BUG
net.sf.jsqlparser.schema.Table.toString方法:
public String toString() {
	return getWholeTableName()+((alias!=null)?" AS "+alias:"");
	修改为：
	return getWholeTableName()+((alias!=null)?" "+alias:"");
	去掉对表名的 AS 关键字，此关键字在oracle中是语法错误，不能执行
}
net.sf.jsqlparser.statement.select.SubSelect
	public String toString () {
		return "("+selectBody+")"+((alias!=null)?" AS "+alias:"");
		修改为：
		return "("+selectBody+")"+((alias!=null)?" "+alias:"");
	}
2.为Update添加toString方法 